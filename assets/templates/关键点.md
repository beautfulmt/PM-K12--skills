v3.1（服务运行时 3.3.0）
【问题】：v3.0 把常驻启动器设成「默认不装」（见下一条第 6 点），于是**自动启动这条链路等于没有**——浏览器点「一键导出」/「一键复制全文」时去叫 launcher_port，那儿没人应答，每次都弹「导出服务没有启动，请双击…」。用户要的是：点导出就自动把服务起起来，不用先双击任何东西。而默认装一个常驻进程到别人机器上，仍然是不合适的。

【思路】：
1. **socket 激活解开这个两难**：plist 里只给 `Sockets`，不给 `RunAtLoad`、不给 `KeepAlive`——**端口由 launchd 自己持有**。空闲时本项目在 `ps` 里一个进程都没有，端口却照样应答；浏览器一连上来 launchd 才把 `prototype_launcher.py` 拉起，它起完服务、空闲 120 秒自退。既是自动启动又不留常驻，所以才敢改成 **macOS 上随安装默认注册**（`install_launcher.sh` 退居重注册/卸载入口；注册失败只警告不中断安装）。取套接字用 libSystem 的 `launch_activate_socket()`（ctypes）；非 launchd 环境下它返回 ESRCH(3)，属预期情况而非错误，据此回退到自己 bind。继承 fd 必须 `HTTPServer(..., bind_and_activate=False)` + 关掉占位 socket + `os.dup(fd)`：自己 bind 会直接撞 EADDRINUSE——那个端口正被 launchd 占着。
2. **在 launchd job 内部调 `launchctl bootout` 等于自杀**（实测踩过一次，是本轮最坏的一个坑）：bootout 杀的就是这个 job 的进程，而那正是**正在处理这次请求的自己**——请求得不到回应，注册还停在半路（bootout 成功、bootstrap 没跑成），`launchctl list` 从此查无此 job，自动启动彻底失效、只能手动重装。防线是 `pm_launchagent.IN_LAUNCHD_JOB`：socket 激活分支里**赶在起服务之前**置上，`sync()` 见到就直接返回。
3. **启动器端口固定，不随端口自愈漂移**：`derive_ports` 的 salt 只作用于服务端口。plist 里端口是写死的，改它只能 bootout + bootstrap，即上面那条自杀路径；让它永不漂移，这个竞态就从根上不存在。真会跟别的应用撞车的本来也只有服务端口。
4. **端口预留不许去探启动器端口**：`reserve_ports` 只探 `config["port"]`。探 launcher_port 必然"bind 不上"——它要么被 launchd 长期持有（socket 激活的实现方式就是如此）、要么被独立运行的启动器自己持有，两种都是**正常状态**；而它又不随 salt 变，一旦判成冲突就是 8 次空转后报「连续多次都没抽到可用端口」。这两个后果都实际撞到了：先是每次启动端口都漂、plist 随即陈旧，后是独立启动器模式下直接起不来。
5. **端口同步只放在 `reroll_ports` 里**——那是全局唯一改动端口的地方。放到调用点上，每新增一条自愈路径都得记得补一次同步，迟早漏；漏一次 launchd 就守着一个没人访问的旧端口，自动启动静默失效。
6. **`--doctor` 要能区分「被 launchd 持有」和真冲突**，否则正常状态会被报成故障。启动器端口若真被外人占了，**不能说"启动时会自动换端口"**（它不会），要给出"停掉占用方后重跑 `install_launcher.sh`、期间双击入口照常可用"的实际指引。
7. Windows 本版仍不做自动启动（已确认的取舍）：服务本体就是用户开着的那个窗口，没有东西需要被拉起。客户端文案按 `navigator.userAgent` 选词，Windows 上照旧提示双击 `.bat` 并保持窗口。
8. 联动更新：README（自动启动条目、「不留常驻进程」条目、§二启动方式、§四改写为按需启动器）、SKILL.md 目录结构、edu-pm-prd.md §3.1-7 与目录树、本文件。
9. **已验证 / 未验证**：macOS 实跑——零进程冷启动 → 点导出 → 5 秒内 2/2 张 PNG 落盘；服务端口被外部进程占用时只有服务端口重抽、plist 与注册纹丝不动；`/api/snapshot` 返回 base64 且二次命中缓存；卸载后 doctor 如实报「未注册」；未注册 LaunchAgent 时独立启动器模式照常工作且不自退；空闲 120 秒自退后端口仍应答、再点导出照常拉起。**Windows 依然没有实跑**，仍只有静态检查。


v3.0（服务运行时 3.2.0）
【问题】：skill 分享给同事后，导出/截图在别人机器上起不来。逐条核实的根因有六个，其中前三个叠加起来完整解释了"双击启动脚本没反应"：① **升级等于没升级**——安装器判定"文件已存在且 hash 不在 installed.json 里 → 视为用户自定义，不覆盖"，而老项目首次升级时 installed.json 是空的，于是**每个既有文件都被判成自定义而保留**，服务代码永远停在旧版；② 因此残留的旧 `.command` 探测固定端口 8765，服务却在别的端口；③ 一个几天前没退出的旧服务仍占着 8765，使②的探测恒真，`.command` 直接 `exit 0` 什么也没做。另外三个是结构性的：④ 缺 `runtime.json` 时在**模块导入期**裸抛 FileNotFoundError，而该目录是 gitignore 的，任何人 clone 项目仓都会撞上且无自救路径；⑤ LaunchAgent 的 label 全局唯一，**一台机器只能服务一个项目**，后装的覆盖先装的；⑥ Windows 完全没有可用路径（没有 `.bat` 入口、安装器是 bash、`python3` 命令不存在、用了 `os.execve`）

【思路】：
1. **安装器按文件分三类，这是①的正解**：运行时代码（`pm_runtime` / `pm_bootstrap` / `start_service` / `prototype_server` / `prototype_launcher` / 导出客户端 / 两个启动入口）**永远覆盖 + 时间戳备份**，不做冲突检测——用户本来就不该手改这些；用户会编辑的（4 份 workflow 说明书、PRD 骨架、绘图提示词）保留冲突检测，新版写进 `.pm-workflow/updates/`；`关键点.md` 和 `.mcp.json.example` 只在缺失时创建。再补 `scripts/known_hashes.json` 收录历代已发布版本的 hash，让"文件等于某个旧发布版"被认成没改过而直接升级，不误判成自定义。另外补上安装器一处都没有的 `chmod +x`（`copy2` 只保留源文件模式，zip/网盘中转会丢权限位 → 双击无反应）和 `.gitattributes`（`*.command` 锁 LF、`*.bat` 锁 CRLF，否则 Windows 检出再传回 Mac 会得到 `bash: $'\r': command not found`）
2. **端口改为按项目确定性推导到 20000–32767，且可自愈**：不再固定 8765/8766，一台机器上多项目各用各的端口；原先用 `bind(port=0)` 抽的是 OS 临时端口段（49152+），恰是系统派给出站连接的范围。启动时端口不可用就换一对重新推导并回写配置。异常处理从子串匹配 `"Address already in use"` 放宽到 `OSError`——Windows 上被 Hyper-V/WSL/Docker 保留的端口抛的是 `WSAEACCES(10013)`，表现为 PermissionError，错误文本里根本没有那句英文。占用者若是本项目自己的服务则正常退出，不能换端口（否则会起出第二个服务）
3. **`ensure_config` 取代 `load_config`，修 ④**：配置缺失时就地创建而不是抛错，导入期不再有可失败的 IO
4. **Windows 路径**：新增 `启动原型导出服务.bat` 和 `init.bat`，正文**必须纯 ASCII**（cmd.exe 按控制台 OEM 代码页解码批处理文本，文件名可以是中文，NTFS 是 Unicode）；解释器按 `py -3` → `python` → `python3` 探测，用**打印哨兵**校验而不看退出码（微软商店的 execution alias stub 会打开商店且不可靠地返回 0）；`os.execve` 换成显式两段式 spawn（Windows 上 execve 是 CreateProcess + 父进程立即退出，会让 `.bat` 窗口一闪即关，且 launcher 的 `poll()` 判活永久失效，真服务崩了也看不出来）；子进程 env 固定 `PYTHONUTF8=1` + `PYTHONIOENCODING=utf-8`（服务打印中文横幅，stdout 重定向进日志后会改用 locale 编码，英文区 Windows 是 cp1252 → 启动期 UnicodeEncodeError）+ `PYTHONUNBUFFERED=1`（否则日志块缓冲，进程被杀时整段丢失——"详见 service.log"最需要它的时候恰好是空的）
5. **运行环境移出项目目录并跨项目共享**：venv 改放 `~/Library/Application Support/pm-workflow/` / `%LOCALAPPDATA%\pm-workflow\`，躲开 OneDrive/iCloud 同步风暴、`os.replace` 的 PermissionError 和 Windows `MAX_PATH` 260 超限。截图引擎优先复用系统已装浏览器（Windows `channel="msedge"` 零下载、macOS `channel="chrome"`），没有才回落自带 Chromium，把最容易被公司代理掐断的 150MB 下载变成可选项。实测复用系统 Chrome、零下载
6. **常驻改显式开启，修 ⑤**：安装时不再自动装 LaunchAgent——在别人机器上偷偷留常驻进程不合适。`setup_prototype_export_watcher.sh` 更名 `install_launcher.sh`（装的是 launcher 不是 watcher），label 改为按项目 `com.pm-workflow.launcher.<project_id>`，支持 `--uninstall`，`KeepAlive` 用 `{SuccessfulExit: False}`（`true` 会无视退出码每 30 秒重启一次，项目删了也照拉）。plist 直接指向项目里的脚本而不复制副本，否则升级后副本不同步、变成一份悄悄跑着的旧代码。Windows 本版不做常驻：服务本体就是用户开着的那个窗口，没有东西需要被守护
7. **新增 `--doctor` 自检**：一次打印解释器与版本、运行环境状态、浏览器引擎、端口 bind 实测、配置是否存在、代理变量。对一个要分享出去的 skill，这条比任何单点修复都省事——把一轮来回排查压成一张截图。localhost 健康检查一律走 `build_opener(ProxyHandler({}))`：urllib 对 127.0.0.1 也认 `HTTP_PROXY`，代理返回的 HTML 会让 `json.load` 抛错 → 判定"没在跑" → 起第二个服务 → bind 失败
8. **报错文案只指向双击入口**——那是两个平台上唯一保证存在于项目里的东西，并按 `navigator.userAgent` 自动在 `.bat` / `.command` 之间选词。历史文案里出现过的 `scripts/initialize.py`（从未安装进项目）、"请重新运行 Skill 初始化脚本"（哪个？）一律删掉
9. 另外修掉两个实现期发现的真问题：`playwright` 包**没有 `__version__` 属性**，用它探测会每次启动都重跑一遍 pip（受限网络下直接卡死），改用 `importlib.metadata.version('playwright')` 并同时 `import playwright` 校验（光有元数据不代表能 import）；后台 spawn 漏传 `env=`，导致后台服务拿不到上面第 4 条的所有编码设置
10. 联动更新：README 新增「安装与启动」章节（Mac/Windows 两条路径 + `--doctor` + 显式装常驻）、SKILL.md 安装指引（**去掉用 bash `find` 定位 skill 的写法**——Windows 上 `find` 是 `System32\find.exe`，参数完全不同，发现环节就会失败，改为用文件搜索工具）、edu-pm-prd.md 的端口与 watcher 表述、目录结构树
11. **已验证 / 未验证要分清**：macOS 上端到端跑通（干净目录安装 → `--doctor` → 起服务 → 真浏览器点导出 → 2/2 张 PNG 落盘带中文名 → snapshot 返回 base64 → 二次命中缓存），R1 回归（铺一份旧布局项目再升级，断言运行时文件全部刷新、手改过的 workflow 仍保留）、`known_hashes` 认旧版、端口自愈（真占坑进程 → 自动换端口并回写）均通过。**Windows 端到端没有实跑**——本版只做了静态检查（`.bat` 全文件非 ASCII 为 0、无 `python3` 字面量、无 `os.execve`、`creationflags` 正确），必须由一位 Windows 同事实跑一次才算数


v2.11
【问题】：拿闲置的结构校验脚本跑真实产出——参考成品全过，最新一份真实 PRD 却 5 项不合格（描述列 `<br>` 硬换行、缺 `.desc-block`、三张表类名不规范），需求目标写成"跑通并验证…"的任务清单。规范写对了，但 2000 行散文约束靠模型每次从零重建 HTML，漂移是必然的。另外三处反馈：双栏预览默认打开挤正文；业务流程图用 Mermaid 默认主题（深紫实心块 + 白字）和 PRD 脱节；交互流程图反复出现文案遮挡。以及一个写法问题：【功能逻辑】按页面写，一条跨三个页面的规则要么抄三遍、要么只在一处出现。

【思路】：
1. **落地保障优先于再写规则。** 新增 `assets/templates/prd-content.html` 骨架（正确类名 / 固定表头 / colgroup / .desc-block / data-preview 占位），§2.2 要求"先复制再填、不从零手写"；新增 `assets/scripts/validate_prd.py` 机械校验（核心章节、项目信息/版本记录分开、无独立异常章、编号连续、六表表头列数、meta 双列、每行 data-preview、原型列非空、desc-block 与 `<br>` 检测、埋点 snake_case 六列、#prdContent 内有正文 style、goal-table 表头）。§5.2 第一步先跑脚本，红了先修；**脚本通过 ≠ 合格，但不过一定不合格**。实测：参考成品 + 骨架 PASS
2. **分块识别规范（§2.3.1）**：两个必出块不变；按需块统一用一个问题判定——"把这一页删掉，这条规则还成立吗"。仍成立的是跨页链路规则（奖励档位、累计口径、到账时机、权限矩阵、全局频控）→ 上提到「三、需求概述」的 h3 规则表只写一次，各格只引用；不写"无"、不写"同上"；一个格子出现四个以上分块就回头核，大概率混进了不属于本页的内容
3. **§2.2 补齐写法**：⑤ 需求概述 = 范围块（本期范围 / 不做什么，接住步骤一确认过的"不做"）+ 功能清单 + 共享规则表；⑥ 需求背景三段式（谁·场景·问题 → 影响 → 证据，无证据明说不编）；⑦ 一行 = 一个界面或状态变体（判据：原型列能否放出一张对应的图）；⑧ 正文末尾「待后续确认的产品口径」集中清单；④ 需求目标补正反对照例。§1.2 加「材料」维度（埋点字典 / 设计规范 / 已有截图，确认阶段问一次）；§1.4 加"确认内容 → PRD 落点"映射表，确认过的"不做"不落进文档等于没确认
4. **双栏预览默认关闭（§2.6）**：body 不带 dual-pane；iframe 的 src 关闭态留空、首次打开才赋值（否则面板没显示原型却在后台加载）；开关按钮两态都有文字（双栏预览 / 收起预览）；唯一自动打开的情形是 `#preview=<page-id>` 深链接，"原型目录非空就自动开"不成立
5. **流程图视觉（§4.1）**：`theme:'base'` + 固定 `themeVariables`（节点 #faf9ff 浅底 / #cbc4e7 细描边 / #30285b 深字 / 线 #8d82b1）、`curve:'linear'` 直角、`nodeSpacing 60 / rankSpacing 70` 为下限、与 PRD 和交互流程图共用页面外壳；节点类型靠形状区分不靠填充色；§4.3 从"语法注意事项"改为交付前逐项核对表（遮挡 / 越界 / 穿节点 / 直角 / 字号 / 风格统一 / 主色一致），并要求 100% 缩放和 PRD iframe 内各看一遍
6. **交互流程图零遮挡三道闸（§4.6 ③④）**：几何下限表（同排 ≥70px、上下排 ≥200px、画布留白 ≥30px、回流轨道每条 ≥28px 错峰、字号下限）且"先确定标签文字再摆节点" → `auditFlow()` 审计 + 修复环（扩画布 → 加间距 → 重排/加轨道 → 拆分组，禁止缩字号 / 删分支 / 用背景色盖）→ 导出前拦截。推荐用内容反推画布尺寸（max(right/bottom)+30），越界从根上消失。实跑一份 13 边的 screenflow：首轮审计 5 项不过，按修复环两轮调 offset / labelT / 改锚点方位后 ok:true
7. 联动：§5.1 交付前必跑脚本并如实报告；§5.2 +7 条；§5.3 +4.1（规则表引用方）/17（待确认清单）/18（重跑脚本）；目录结构；SKILL.md PRD Rules 同步


v2.10
【问题】：① 一份参考 PRD 成品的格式（固定章节顺序、各表固定表头、需求目标写法、`--pm-accent` 主色变量、文档级 style 放正文容器内）只活在成品里，工作流规范没写死，从零写每次都会漂；② 编辑模块删掉一行 / 一张表后没有任何退路，只能重开文件丢掉所有未保存修改——浏览器对 contenteditable 的原生撤销只管文字，管不了增删行、删表和尺寸拖拽

【思路】：
1. **格式规范整段固化（§2.2–2.5）**：固定顺序 项目信息 → 版本记录 → 背景 → 目标 → 概述 → 业务流程图 → 交互流程图 → 详细方案 → 埋点 → 时序图（上线计划 / 附录确有需要才补）；项目信息与版本记录必须两张独立表；**删除独立「异常与边界」章**，边界一律进对应功能的【边界说明】；各表表头文案与列数不可改；需求目标写"用户结果 + 衡量口径"，无基线写「待基线确认」绝不编数；埋点 snake_case、沿用字典、曝光/点击/结果分开
2. **原型列两种形态都合法**：高清截图 `img.proto-shot`（真实 width/height + `asset-note` 链到活原型）或 iframe，不强制把截图换成 iframe；`data-preview` 写在每个 `<tr>` 上（rowspan 合并时不漏行）
3. **版式（§2.5）**：微软雅黑字体栈 + 后备字体据实说明；标题层级与章节竖线；`--pm-accent` + `color-mix()` 推导标题色 / 描边 / 表头底色，换主色只改一处；**文档级 `<style>` 放 `#prdContent` 内第一个子元素**（一键复制全文抓的是这个容器，样式在容器内才带得走），骨架样式留 `<head>`
4. **撤销 / 重做（§2.7 ⑤）**：`⌘Z / Ctrl+Z` 撤销，`⌘⇧Z / Ctrl+Y`（兼容 `Ctrl+⇧Z`）重做；文字（连续输入合并一步）/ 增删行 / 删表 / 列宽行高 共用一个栈，新编辑清空重做分支，上限 200 步，不承诺跨刷新。关键实现点：**删除时保活 DOM 节点对象**（不是存 HTML 字符串），否则恢复出来的行控件是死的；**捕获阶段 `preventDefault` 拦掉原生 undo**，否则两套栈叠加撤两步；`applying` 标志位防恢复动作再入栈；撤销前先结算正在输入的一笔；文字步连 `data-changed` / `.edited-cell` 一起存取；toast 用完即摘除，保存 / 复制排除名单不用改。无头 Chrome 34 项实测全绿
5. 联动：§1.3.1（可选章节 8 → 7）、§3.3、§5.2、§5.3 表项重排；SKILL.md PRD Rules 同步


v2.9
【问题】：PRD 会被推到公司内网用 http 访问，而 navigator.clipboard / ClipboardItem / showSaveFilePicker 这些能力要求"安全上下文"（https 或 localhost），内网 http 下直接是 undefined；导出服务原来的路径解析只认绝对路径，http 下 location.pathname 是站点根路径、磁盘上不存在，导致 /api/snapshot、/api/asset 全部 400，一键复制全文的原型图从有变成全无。

【思路】：
1. 复制统一走 execCommand 兜底：navigator.clipboard 不存在时降级用离屏 textarea + document.execCommand('copy')
2. 保存能力对应降级：showSaveFilePicker 不存在时改为触发浏览器下载一份副本，不抛错阻断
3. 导出服务 _resolve_base_html() 补一次按项目相对路径的解析，不再只依赖绝对路径匹配，http 下也能定位到调用方 HTML 文件
4. 联动更新：edu-pm-prd.md §2.9（内网 http 不安全上下文的真实边界）；SKILL.md PRD Rules 交叉引用同步


v2.8
【问题】：「一键复制全文」已经能带出原型图了，但 PM 反馈粘进文档后**图片发虚、质量不高**

【思路】：
1. **先量再改，别照直觉动手。** 直觉是"JPEG 0.85 压太狠"，实测把同一素材同一区域做四档对照——`JPEG .85` / `JPEG .95` / `PNG 无损` / `WebP .92`，**全部缩到 1200 后几乎看不出差别**，而不降采样的原图明显锐利。结论反过来了：**发虚是 `MAX_IMG_WIDTH = 1200` 那一步降采样丢的细节，不是 JPEG 压坏的**，换编码格式（哪怕换成无损 PNG）一点用都没有
2. **验收姿势本身是个坑。** 图在文档里的显示宽度被 §2.8 ④ 固化成表格「原型」列的宽度（实测 335px），**这个尺寸下 1200 和 2880 原图肉眼完全一样**——按默认大小看永远发现不了问题，差异只在读者放大看细节时暴露。所以 §5.2 加了一条：验收必须放大到 1000px 以上再看
3. **拐点靠体积曲线选，不拍脑袋。** 9 张真实素材实测：`1200/.85 = 0.90MB`、`1600/.9 = 1.65MB`、`2000/.9 = 2.20MB`、`2880 原尺寸/.9 = 3.60MB`。取 **2000 + 质量 0.9**——放大到 1500px 仍锐利，体积只有原尺寸的六成
4. **报体积要报剪贴板里的量。** 上面那些是解码后字节数，剪贴板里躺的是 base64 文本，**再膨胀 1.33×**。这份 PRD 实测：图片解码 2.83MB → 剪贴板 HTML **3.82MB**（改前 1.65MB）。按解码值向用户承诺会低估三分之一，粘不粘得进去看的是后者
5. WebP 更省（`2000/.9` 仅 1.34MB）且质量更好，但**钉钉/飞书/Word 对剪贴板内 base64 WebP 的支持没实测过**，核心场景不赌，留作已知选项写进规范
6. 联动更新：edu-pm-prd §2.8 ⑤（重写体积控制 + 三条踩坑）/ §2.8 取材限制（体积口径）/ §5.2（+1 条放大验收）/ 项目版 workflow 同步；真实 PRD 常量改 2000 + 0.9；回归 `file://` 39 + http 12 + 正文零丢失比对全绿


v2.6
【问题】：① PRD「📋 一键复制全文」直接 `iframe.remove()`，复制到钉钉/飞书/Word 后**原型和流程图全部消失**，PM 每次都要手动逐张截图重贴；② 右侧双栏预览恒等于"适配面板宽度"，横屏 1280×720 缩进 520px 面板后细节看不清，也没法放大局部

【思路】：
1. 复制出图必须**走本地 8765 导出服务出 base64**——`file://` 下浏览器既拦本地 `fetch`、canvas 又会被跨源污染，前端截不出图；与 skill 既有"导出必走本地服务"同源，不新造 html2canvas 路径
2. 服务端新增 `POST /api/snapshot`（缓存优先＋自动重渲）：截图收尾写 `.export-manifest.json` 建立 `page_id → PNG` 映射；命中条件＝清单有该页 + PNG 存在 + `png.mtime >= html.mtime`，否则只渲这一页再落盘。用**独立锁**，不占 `/api/screenshot` 的 `state.running` 状态机，复制全文不会搅乱整轮导出进度条
3. 取图要能认全四种真实 iframe（踩坑：一开始假设原型一定有 `.device[id]`，实测 `-chapter.html` 是 `.screen` 单屏页直接失败，且 `_discover_pages` 无 `.device` 时是**抛异常**不是返回空）→ 改成 A/B/C 三档阶梯：A 流程图 `.chart-container` 按 `{feature}-图n`、B 平铺原型走 `_discover_pages`（try/except 兜住）、C 兜底 `.chart-container/.device/.screen/.phone/main/body` 独立命名
4. **query 必须保留**（踩坑：PRD 里流程图 iframe 是 `?only=flow-main`，旧的 `_split_hash` 直接按 `?` 切掉，取出来的图跟 PRD 里看到的不是同一张）→ `_split_ref` 返回三元组，query 专属条目在清单里用 `q:` 前缀自成一档缓存
5. `scale` 默认 **2**（对齐整轮导出的 `EXPORT_DEVICE_SCALE`，保证同一份文档里图片清晰度统一，下采样需要额外图像库故不做），可按需传 1 换体积；`img` 绝对化那步要**跳过 `data:`** 否则把 base64 改坏
6. 三档降级据实告知：全成功／部分失败（成功贴图、失败删掉并报数）／服务不可用（退回删 iframe 并明说"原型图缺失"），禁止静默丢图；口径上只承诺"base64 不依赖本地路径"，**不承诺目标文档一定内联**——首次接入某个文档必须人工粘一次确认
7. 双栏缩放沿用 §4.6 控件组（`－/%/＋/适配`，去掉 📸），档位 25/50/75/100/125/150/200/300，从适配态起跳以当前适配倍率为起点不猛跳；溢出交给已有 `.device-shell{overflow:auto}`，**不做拖拽平移**（侧栏是活原型，拖拽会和原型自身点击打架）；`Ctrl/⌘+滚轮` 只作补充并如实记"指针在 iframe 内时事件归 iframe、父页收不到"；切页/scroll-spy/拖 resizer 均**保留缩放模式**；`previewZoom` 只存 JS 变量不写 DOM（守 §2.7 不落 `data-*` 的约定）
8. **导出清理绝不许通配符清场（血的教训，删了 8 张补 1 张）**：`_run_screenshots` 原来对原型用 `*.png` 清理"上一轮产物"，实测把 `原型截图/` 里 PM 手工做的 8 张图整目录删空。那些图是用 `[需求名]-shots.html`（状态可寻址截图版）按 hash 一状态一张手截、手工命名（`01-详情页.png` 等），PRD 的「原型」列直接引这些名字，而触发删除的 `-prototype.html` 没有 `.device` 只出 1 张图，**且这些 PNG 不在 git 里，删了不可恢复**。改为只删本 HTML 在 `.export-manifest.json` 里登记过的文件名（流程图额外保留 `{feature}-图*.png` 这条工具独占命名的兜底），并加"只删本目录直接子文件"防 `../` 越界。截图目录是 PM 的资产目录，不是工具的临时目录
9. 联动更新：edu-pm-prd §2.6①②③⑨ / §2.8 全节 / §4.4 第 6 条（截图目录清理禁令）/ §5.2（+2 条）/ §5.3 第 16 项；`prototype_server.py`、`prototype-export-client.js`；SKILL.md PRD Rules +2 条


v2.5
【问题】：研发/测试拿 PRD 写逻辑/写用例时缺一张"端侧互动编排"级别的图。流程图只到业务层，动效播放顺序、录音开关时机、请求返回门控、重试口径等端侧编排细节没有统一表达位；某实时交互功能的网络异常口径与实际编排也曾出现不一致。

【思路】：
1. 新增可裁剪章节「九、时序图」（研发/测试向，edu-pm-prd 新增 §4.7），**位于数据埋点之后（正文最后）**；固定两图＝端侧互动编排总时序（sequenceDiagram，泳道按实际参与方如 用户/端/Cocos/服务端）+ 关键对象状态图（stateDiagram-v2，如角色动效状态机），每块下附「说明」要点；裁剪判据＝无复杂端侧编排（无语音/流式/多端协同/复杂状态机）的简单需求省
2. **形态＝Mermaid 源码文本块（不渲染成图）**：<pre> 等宽深色代码块 + 每块右上角「📋 复制源码」按钮，研发照文本写逻辑、测试照文本写用例、可整段复制到任意 Mermaid 工具二次使用（踩坑：首版做成渲染图+iframe 被打回——图片研发不好复用）；不产出独立文件、不占 flow.html
3. 书写规范：动作/资源名直接复用真实资源目录名；关键门控用 Note 标注（如 control 先返回也要等 done 播报完再推进）；技术字段豁免（§2.3.1 禁令只管详细方案描述列）；交付前源码仍须在 Mermaid 环境验证无 Syntax error
4. 章节全序＝背景/目标/概述/流程图/交互流程图/详细方案/异常/埋点/**时序图(九)**/上线(十)/附录；1.3.1（8 个可选）/2.2/步骤四前置判断/5.1/5.2/5.3（17 项，时序图列第 11）联动更新；SKILL.md 同步
5. 复杂实时交互需求实跑：PRD 新增时序图文本块与复制按钮；网络异常口径对齐为「任意网络异常 → 弹不可取消重试弹窗 → 点击重试复用当前轮已有输入」，并同步详细方案与主流程图边界说明


v2.4
【问题】：① 详细方案「描述」列还是松散的 0/1/2 编号叙述，页面元素、交互、规则、边界糊在一起，评审不好读，偶尔还混进技术接口说明；② PRD 缺一张"所有核心界面拼成一整张带箭头跳转链路"的交互流程图（screen flow），评审者要在脑中自己拼链路，状态机（首次/非首次、上传中/成功等）也没有统一表达位

【思路】：
1. 描述列固化为「分块结构」（新 §2.3.1）：【页面元素】【交互说明】每行必出；【功能逻辑】【边界说明】【数据与内容规则】【前置条件与权限】【文案规范】按需出、不硬凑不写"无"；硬性禁令＝描述列不得出现任何技术接口表述（API/参数/字段key/错误码），一律产品语言
2. 新增可裁剪章节「五、交互流程图」，插在流程图之后、详细方案之前；与 Mermaid 流程图并存各管一层（Mermaid＝业务/时序/状态逻辑，screenflow＝界面跳转链路）；裁剪判据＝核心界面≤2 或无跳转链路且无状态流转
3. 实现（新 §4.6）：独立 HTML `[需求名]/流程图/[需求名]-screenflow.html`，界面节点用缩放 iframe 引真实原型 hash 页（原型改动自动同步、禁截图拼贴）；箭头用 EDGES 数据驱动 + JS 读节点位置动态画 SVG（禁手写死坐标），连线一律**直角折线**（Manhattan 路由，禁贝塞尔曲线/斜线），同侧长回环走 bend 错峰"轨道"、锚点可 offset 防重叠；画布带右上角 ＋/－/适配 缩放控件 + 拖拽平移（嵌入 PRD 默认适配宽度、独立打开默认 100% 保导出原尺寸），底部轨道标签下留 ≥30px 防裁切；「📸 下载图片」按钮并入缩放控件组（保留 .export-btn 类走服务拦截），嵌入 PRD 也可直接下载 PNG；画布带 chart-container 类复用 Playwright 导出服务出整张 PNG
4. 状态机必做：同一界面不同状态拆独立节点、每条边标"触发动作+条件"、画布角落放状态机图例，与 PRD 状态定义一致
5. 章节编号全文档重排（详细方案五→六、异常六→七、埋点七→八、上线八→九），1.3.1/2.2/5.1/5.2/5.3（插入第6项、共16项）/目录结构联动更新；SKILL.md 同步；两份 canonical 副本一致


v2.3
【问题】：双栏预览的右侧交互原型要手动下拉切页，看 PRD 时左右对不上——读到「界面3」还得自己去下拉切到界面3

【思路】：
1. 加滚动联动 scroll-spy：左侧正文滚到某模块，右侧预览自动切到该模块对应原型页
2. 取材＝给详细方案每个「一级模块」单元格加 data-preview=<CATALOG value>；监听 window 滚动(rAF节流)，取视口上方约35%「激活线」之上最后一个 data-preview 元素为当前模块，v!==previewPage 时才 switchPage（避免反复重载 iframe），仅 dual-pane 打开时生效
3. 不打架：手动用下拉(#pageSelect change)后约4秒暂停联动；switchPage 程序化改 select.value 不触发 change，故自动切不会误锁
4. 固化进 edu-pm-prd §2.6 ⑧ + 5.3 第14项迭代清单（新增模块/页要同步补 data-preview）
5. 已用无头 Chrome 验证：滚到 record/guide/report/feedback/chapter 预览逐一跟随、手动锁定 4s 生效


v2.2
【问题】：新生成的流程图 HTML 导出截图只截了一半。长流程图比视口高，按钮直接用浏览器端 html-to-image 截 `.chart-container`，受画布和视口限制被裁断；项目已有 Playwright 本地导出服务，却没有接上

【思路】：
1. 根因：流程图 HTML 漏接 `scripts/prototype-export-client.js`，退化成裸 html-to-image。修复为统一接入导出服务客户端（嵌套 `[需求名]/流程图/` 下路径为 `../../scripts/`，旧扁平结构为 `../scripts/`）
2. 客户端在捕获阶段拦截 .export-btn 点击 → POST localhost:8765/api/screenshot → Playwright 对每个 .chart-container 整元素截图 → 输出 [需求名]/流程图截图/；服务没起会自动 launcher 唤起、否则提示双击「启动原型导出服务.command」
3. onclick 仅作降级：必须服务优先 window.exportPrototypeViaServer，失败再 html-to-image 且按完整 scrollWidth/scrollHeight+canvasWidth/Height 截，避免半张
4. 固化进 edu-pm-prd 新增 §4.5「导出截图必走本地导出服务（必做）」+ 产出后自检；两份 canonical 副本同步
5. 已用无头 Chrome 验证客户端从 ../../scripts/ 正确加载、window.exportPrototypeViaServer 就绪、按钮 class=export-btn 被识别


v2.1
【问题】：PRD 写完后想整篇拷到钉钉/飞书/Word，手动全选会把右侧预览面板、悬浮按钮、拖拽手柄、绿色编辑高亮和 contenteditable 脏属性一起带出去，很脏

【思路】：
1. 左下角固化一个「📋 一键复制全文」按钮（edu-pm-prd 新增 §2.8），与右下角保存按钮分居两侧
2. 复制为富文本 text/html（保留表格/标题/加粗/列表）+ text/plain 兜底；优先 Clipboard API，被拒回退离屏选区 execCommand
3. 导出前在 body 副本上清洗：删交互骨架（预览面板/fab/各手柄/iframe/script）、解包 editable-table-wrap、去 contenteditable/edited-cell/data-* 痕迹、给表格补内联边框、图片 src 绝对化
4. 据实标注取材限制：本地相对路径截图粘到云文档通常不自动上传，复制以文本+表格结构为主
5. 已用无头 Chrome 实测，清洗与剪贴板负载断言全部通过；随后固化进两份 canonical 副本


v2.0
【问题】：PRD 需求文档只有「五、详细方案」的描述列能编辑、也只有详细方案表能增删行；其余正文（背景/目标/版本记录/埋点…）在浏览器里都改不动；且表格列宽行高不能拖拽调整（旧 2.5 还明令禁用 CSS resize 怕截图残影）

【思路】：
1. 固化「通用编辑模块」为 PRD 必做项（edu-pm-prd 新增 §2.7）：一段自包含 JS 一次性提供 ①全文档可编辑（td/th/p/li/h1~h4/.alert… 自动 contenteditable + blur 变更追踪 + 绿色 edited-cell）②全表增删行（所有数据表 body 行 hover 出 ＋/−，表头行除外）③列宽/行高专用拖拽手柄
2. 列宽改 `<colgroup><col>` 宽（下限 48px）、行高改 `tr` 高（下限 24px）；手柄一律 hover-only + 写 inline px → 随 outerHTML 保存且静态截图无残影，**替代旧 §2.5 的 resize 禁令**（保留「别用 CSS resize」的结论，改走专用手柄）
3. 排除项保证交互骨架不被误编辑：右侧预览面板/悬浮按钮/各手柄/iframe/script/button；含 iframe 或嵌套表的单元格（详细方案原型列）整体不接管
4. 模块幂等可重复 init；迭代新增表/正文后调 `PMEdit.init(新节点)` 即生效；某表加 `class="no-edit"` 可豁免
5. 范围：本次只更新 skill 规范（不回填现有 5 个 PRD HTML）；模块已用无头 Chrome 实测 21 项断言全通过
6. 更新到 edu-pm-prd 的 §2.4/§2.5/新增 §2.7，两份 canonical 副本（.agents/workflows + skill assets）保持一致


v1.9
【问题】：PRD 章节原本是 11 章全固定必出，小需求（如纯文案调整）也被迫硬塞流程图、灰度策略等用不上的章节，文档冗余

【思路】：
1. 章节从「固定必出」改为「按需求大小裁剪」：核心骨架（项目信息+版本记录、需求背景、需求目标、详细方案）始终保留；其余 6 章（需求概述/流程图/异常边界/数据埋点/上线计划/附录）按需求复杂度可整章省略
2. 保留章节的相对顺序不变，编号跟随实际保留章节连续重排，不跳号
3. 不得默默省略：在需求确认阶段（1.3.1）先列出本次保留/省略哪些章节及原因，经用户确认后再写
4. 产出物联动：若省略「流程图」章节，则整步跳过、不产出流程图 HTML 文件
5. 迭代时若新需求触及了之前省掉的章节，需先告知用户再补回该章节并按序插回
6. 更新到 edu-pm-prd（2.2/1.3.1/步骤四/5.2/5.3）+ SKILL.md


v1.8
【问题】：产物原本按「类型」平铺在根目录（需求文档/原型/流程图/…），所有需求混在同一批文件夹里，需求一多就分不清哪些文件属于哪个需求

【思路】：
1. 改为按「需求名」嵌套：项目根目录下先以需求名建一个顶层文件夹，该需求的 需求文档/原型/流程图/原型截图 都作为它的子目录，不同需求互不混放
2. scripts/、启动原型导出服务.command、.handoff/ 仍为所有需求共享，留在项目根目录
3. 受影响的相对路径：原型 HTML 引用导出脚本由 ../scripts/ 改为 ../../scripts/（多嵌套一层）；PRD↔原型↔流程图 仍同级，iframe src 不变
4. 导出服务 prototype_server.py 改为扫描 [需求名]/原型/*.html（兼容旧扁平 原型/*.html），截图按原型所在位置输出到 [需求名]/原型截图/（旧结构回退根 原型截图/）
5. init.sh 不再预建产物类型目录，只建共享的 scripts/ 和 .handoff/，需求子目录写需求时按需创建
6. 旧的扁平产物保持原位不动，新旧结构并存
7. 数据分析/需求挖掘/验收清单 一并纳入同一需求目录：一个 [需求名]/ 下最多含 7 类子目录（需求文档/原型/流程图/原型截图/需求挖掘/验收清单/数据分析）+ 沟通记录.md
8. 建立统一「需求目录归属规则」：谁先开工谁建家——任何工作流落盘前先查根目录是否已有同名 [需求名]/，有则复用、无则新建；不假设 PRD 先行，先做需求挖掘/数据分析也能独立以需求名建家，后续步骤复用同一目录；子目录用到才建，不预建空文件夹
9. 更新到 edu-pm-prd / demand / acceptance / data-analysis / SKILL.md / init.sh / prototype_server.py


v1.7
【问题】：skill 和 workflow 存在多份副本，且部分 PRD 规则带有旧后台口径、错误保存对象、表格 resize 残影风险，容易导致不同 agent 读到不同规范

【思路】：
1. 将 `.agents/workflows/edu-pm-prd.md` 作为项目运行时权威规范，skill assets 作为未来安装模板，根目录 `edu-pm-prd.md` 只作为兼容副本
2. 修正 PRD 保存按钮规则：保存当前 PRD HTML 文件，不写成覆盖原型
3. 去掉默认 `th/td resize: both` 规则，避免表格残影和截图问题
4. 迭代清单从固定 C端/后台 改为“按当前需求实际存在的端组织”，删除 `adminPages` 硬编码
5. `init.sh` 对 workflow/helper 文件采用“更新并备份”策略，避免升级 skill 后旧项目继续使用旧规范


v1.7
【问题】：原型导出服务之前用 5 秒轮询 LaunchAgent 守护，只要端口空就自动拉起完整服务（含 Playwright/Chromium），导致用户关闭终端后它又被拉起，占用资源且用户不感知

【思路】：
1. 把"常驻"拆成两层：极轻 launcher（~5MB，只监听 8766 端口）+ 完整导出服务（8765，按需拉起）
2. launcher 唯一职责：收到 POST /api/launch 时用 `open` 唤起「启动原型导出服务.command」
3. LaunchAgent 只拉 launcher，不再轮询 8765 端口
4. 浏览器 JS 新增降级：先 ping 8765，失败就 POST 8766 让 launcher 去拉完整服务，再等 30s 服务起来继续导出
5. 解决 LaunchAgent 沙箱读不到 ~/Documents 的问题：把 launcher.py 装到 ~/Library/Application Support/pm-prototype/，项目目录通过环境变量 PROTOTYPE_PROJECT_DIR 传入
6. 更新到 edu-pm-workflow assets（scripts/prototype_launcher.py、setup_prototype_export_watcher.sh、prototype-export-client.js）


v1.6
【问题】：新增需求的时候经常只更新"直接提到的那一处"（如只改详细方案），忘了同步"需求概述功能清单表""埋点""异常处理""决策对齐表"等其他章节，导致 PRD 前后不一致

【思路】：
1. 在 edu-pm-prd.md 的"5.3 迭代更新规则"里写死一张"变更后必须遍历的 15 个位置清单"
2. 清单覆盖：版本记录 / 功能清单 / 流程图 / 各实际存在端的详细方案 / 异常处理 / 埋点 / 上线计划 / 决策对齐表 / 原型 pages 数组 / 双栏预览下拉项 / 页面映射 等
3. 规则本体强调"反向规则"：原型/流程图的独立变更，也要回查 PRD 是否需要跟改
4. 每轮迭代交付时必须在回复里明确列出"本次同步更新了哪些位置"
5. 更新到工作流 5.3 节


v1.5
【问题】：原型交互体验不够好，多个关联页面（如报告的三个Tab）跳转后Tab栏消失、PRD与原型联动不够丝滑

【思路】：
1. 建立 Tab 切换模式规范：多个平级子页面合并为单页+Tab面板，点击Tab只切内容不跳页
2. 建立 PostMessage 双向通信协议：PRD页面选择器 ↔ 原型 iframe 实时同步
3. 支持 tabAliases 映射：旧页面名自动重定向到对应Tab，向后兼容
4. 更新到工作流 3.4 节


v1.4
【问题】：HTML原型视觉效果一般，想接入 Pencil 设计工具画更精美的高保真原型

【思路】：
1. Pencil 作为可选增强，不作为必须依赖——没装Pencil也能跑通整个工作流
2. HTML原型完成后主动询问用户是否需要Pencil增强，让用户选择
3. 自动检测Pencil安装状态，未安装给安装引导，已安装检测MCP连接
4. 建立统一设计变量体系（$primary、$accent等），Pencil和HTML两端保持一致
5. 新增 HTML↔Pencil 视觉一致性校准步骤，避免PRD中左右截图不一致
6. 新增 MCP 连接故障排查方案（含WebSocket直连降级）
7. 更新到工作流 3.5 节


v1.3
【问题】：PM的工作流里其实还缺失一些需求挖掘（市场、竞品、数据等）、验收清单、数据分析

【思路】：
1. 保持原有风格，在工作流里新增各个步骤的md独立工作流
2. 最终的产出也分开独立的文件夹
3. 整体打包
4. 大家可以根据自己的风格去让AI去生产你自己风格的工作流，可以告诉模型你要改哪个md文件，以及怎么改







v1.2
【问题】：原型图现在是html格式的，如果想直接用只能再去截图不是非常方便

【思路】：
1. 是否可以直接让模型给改成html画完后变成图片格式
2. 改成图片后会有什么问题吗？还方便改动吗？
3. 保持html格式的原型，这样方便改动，增加一键导出截图的功能
4. 更新工作流md文档


v1.1

【问题】：我就担心文案上或者小细节要调整，再告诉ai调整反而步骤变多了

【思路】：
1. 首先需要先要让html从不可修改变成可修改
2. 直接在html修改文档逻辑，修改完后保存html
3. 让模型根据本次prd里的修改对原型进行重新设计


v1.0
【主题】：搭建Prd工作流
【效果】：你只需要给模型几句话AI生成你风格的PRD
【关键问题】：
1. 你是谁？
2. 你的工作边界是什么？
3. 你需要模型帮你做什么？
4. 将你自己写的还不错的文档罐给模型让它学习
5. 有迭代后记得让它更新到你的工作流md里
