# 教育PM需求产出 — 核心Prompt手册

> 本文档整理了使用AI完成「需求描述 → PRD + 原型 + 流程图」全流程所需的**核心Prompt**。
> 直接复制对应阶段的Prompt发给AI即可使用。

---

## 使用前准备

确保以下文件已就位：

```
PM工作流/
├── PRD模板.md                          # PRD参考模板
├── .agents/workflows/edu-pm-prd.md     # 完整工作流定义
├── 需求文档/                            # PRD输出目录
├── 原型/                                # 原型输出目录
└── 流程图/                              # 流程图输出目录
```

---

## Prompt 1：启动工作流

> **使用场景**：开始一个新需求时，一句话触发整个流程。

```
/edu-pm-prd

我有一个新需求：[一句话描述你的需求]

补充信息：
- 产品：[产品名，如XX学生端App]
- 场景：[课前/课中/课后]
- 目标用户：[学生/家长/老师]
- 核心目标：[想解决什么问题、达成什么效果]
- 参考竞品（如有）：[竞品名或链接]
```

**示例**：
```
/edu-pm-prd

我有一个新需求：在学生端App中上线「AI名师讲题」功能，学生拍照上传题目，AI以虚拟老师的身份用语音+文字讲解。

补充信息：
- 产品：XX学生端App
- 场景：课后
- 目标用户：K12学生和家长
- 核心目标：为引流课增加差异化增值服务，提升转化率
- 每日免费5次使用限制
```

---

## Prompt 2：生成PRD

> **使用场景**：需求确认后，让AI产出完整PRD。

```
请按照工作流「步骤二」生成这个需求的PRD，要求：

1. 输出为 HTML格式（非Markdown），保存到 需求文档/[需求名]-PRD.html
2. 内嵌所有CSS，脱机可用
3. 章节顺序：项目信息 → 版本记录 → 需求背景 → 需求目标 → 需求概述 → 流程图 → 详细方案 → 异常处理 → 数据埋点 → 上线计划 → 附录
4. 详细方案必须用四列表格：一级模块 | 二级功能 | 原型 | 描述
5. 一级模块用rowspan合并，描述用编号叙述法（0、功能说明 / 1、xxx / 2、xxx）
6. 原型列暂时使用占位符 <div class="photo-placeholder">📸 截图占位</div>
7. 页面最大宽度1400px，表头支持拖拽调节列宽
```

---

## Prompt 3：生成交互原型

> **使用场景**：PRD内容确认后，让AI制作原型。

```
请按照工作流「步骤三」制作原型，要求：

1. HTML单文件，所有页面状态通过 URL hash 切换（如 #home, #camera, #loading, #result-success, #error-unclear, #history）
2. 无 hash 打开时，默认平铺展示全部页面状态，便于直接截图和评审；带 hash 时只展示对应单页
3. 技术栈：Tailwind CSS + Font Awesome（CDN引入）
4. 原型容器尺寸需与实际业务端一致，课中场景优先按课堂真实比例设计，不要默认套手机竖屏
5. <body> 无 hash 时支持多页面平铺预览；iframe 单页预览时避免上下白边
6. 保存到 原型/[需求名]-prototype.html

需要覆盖的页面状态：
- [列出你需要的所有页面，例如：首页Tab入口、AI老师主页、切换老师面板、次数用完弹窗、相机页、Loading态、成功结果页、失败提示页、答疑历史]
```

---

## Prompt 4：生成流程图

> **使用场景**：和原型一起或单独生成流程图。

```
请按照工作流「步骤四」制作流程图，要求：

1. 使用Mermaid语法，保存为独立HTML文件
2. 用sequenceDiagram绘制多角色交互时序图（用户 → 客户端 → 服务端 → AI）
3. 覆盖正常流程和所有异常分支
4. 保存到 流程图/[需求名]-flow.html

注意：如果Mermaid语法报Syntax error，先验证再嵌入；如果某段图渲染失败就删除该段。
```

---

## Prompt 5：将原型和流程图嵌入PRD

> **使用场景**：原型和流程图都做完后，替换PRD中的占位符。

```
现在请把原型和流程图嵌入到PRD中：

1. 将详细方案表格中每一行的 <div class="photo-placeholder"> 替换为对应的 <iframe>
2. iframe格式：
   <iframe src="../原型/[需求名]-prototype.html#[页面hash]" 
     style="width: 100%; height: 570px; border: none; background: transparent;"></iframe>
3. 流程图部分也用iframe嵌入：
   <iframe src="../流程图/[需求名]-flow.html" 
     style="width: 100%; height: 800px; border: none; background: transparent;"></iframe>
4. 确保没有双重边框和上下白边
```

## Prompt 6：完整一键式Prompt（高级用法）

> **使用场景**：如果你想用一个Prompt跑完全流程（需要AI能力较强的模型）。

```
/edu-pm-prd

需求描述：[你的需求描述]

请按照工作流完整执行以下步骤：
1. 先和我确认需求理解是否正确
2. 确认后生成PRD（HTML格式，四列表格，章节顺序：背景→目标→概述→流程图→详细方案→异常→埋点→上线）
3. 生成交互式原型（HTML单文件，hash路由切换页面）
4. 生成Mermaid流程图（时序图为主）
5. 将原型和流程图通过iframe嵌入到PRD对应位置
6. 所有文件保存到对应目录

全程中文输出。
```

---

## 常见问题 & 排错

| 问题 | 解决方案 |
|------|----------|
| Markdown粘贴到钉钉格式丢失 | 改用HTML格式输出，浏览器打开后截图粘贴 |
| 原型上下有白边 | body去掉 `items-center min-h-screen`，iframe用 `border:none` |
| 原型文件直接打开时只看到一个页面 | 调整为“无 hash 展示全部页面，带 hash 只展示对应页面” |
| 流程图报 Syntax error | Mermaid对中文括号/引号敏感，删除报错段或简化标签文字 |
| 表格列太窄文字挤压 | 页面max-width改为1400px，th加 `resize:horizontal`，td加 `word-break:break-word` |
| iframe显示空白 | 检查相对路径是否正确（PRD在 `需求文档/`，原型在 `原型/`，需要 `../原型/`） |

---

## 交付到钉钉的SOP

1. 在浏览器中打开 `[需求名]-PRD.html`
2. 对整个页面或所需部分使用截图工具（Mac: `Cmd+Shift+4`）
3. 直接 `Cmd+V` 粘贴到钉钉文档
4. 原型和流程图的HTML文件作为附件上传到钉钉
